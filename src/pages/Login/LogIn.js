import React from 'react';

const LogIn = () => {
  return (
    <div className="login"
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Log In</h1>
    </div>
  );
};

export default LogIn;
